name: Docker Image CI

on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: 🖌 Log in to Docke hub
      uses: docker/login-action@65b78e6e13532edd9afa3aa52ac7964289d1a9c1
      with:
        registry: asm0day
        username: asm0day
        password: dckr_pat_c8Bouwt-UzLZy4cDcLNAL60MS4k
          
    - name: 📦 Build the Docker image
      run: docker build -t asm0day/user-test-nestjs-mongodb:latest .
